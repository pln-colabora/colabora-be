# COLABORA Roadmap

The canonical status, delivery phases, dependencies, and exit gates live in [`DEVELOPMENT.md`](./DEVELOPMENT.md).

Current sequence:

1. Canonical workflow engine — complete.
2. Workflow-node persistence — next.
3. Connection-specific entry path and node-based read model.
4. Activity write endpoints in dependency slices.
5. Evidence and access hardening.
6. Integration readiness and frontend handoff.

Before starting any phase, re-read both living diagrams under `hifi-colabora/workflow/` and synchronize the target contracts when the diagrams have changed. Runtime OpenAPI under `docs/` must continue to describe implemented behavior only.
