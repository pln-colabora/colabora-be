# Copy to .env and fill in real values. Never commit .env (see .gitignore).

# App
APP_NAME=colabora-be
APP_ENV=localhost           # "localhost" binds 0.0.0.0:$GOLANG_PORT; anything else binds :$GOLANG_PORT
GOLANG_PORT=8888
NGINX_PORT=81                # only used by docker-compose's nginx service

# Database (PostgreSQL)
DB_HOST=localhost
DB_USER=user
DB_PASS=password
DB_NAME=db
DB_PORT=5432

# Auth
JWT_SECRET=change-me-to-a-long-random-string

# Email (SMTP) — used for verification/password-reset emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SENDER_NAME=COLABORA
SMTP_AUTH_EMAIL=your-email@gmail.com
SMTP_AUTH_PASSWORD=your-smtp-app-password
